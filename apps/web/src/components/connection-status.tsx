import { CloudOff, LoaderCircle, Radio, ShieldAlert } from 'lucide-react';
import { cn } from '@/lib/utils';
import type { ConnectionPhase } from '@/features/realtime/realtime-match-reducer';
const details: Record<ConnectionPhase, { label: string; className: string; icon: typeof Radio }> = { IDLE: { label: 'Chưa kết nối', className: 'text-slate-500', icon: CloudOff }, CONNECTING: { label: 'Đang kết nối', className: 'text-amber-700', icon: LoaderCircle }, SYNCING: { label: 'Đang đồng bộ', className: 'text-amber-700', icon: LoaderCircle }, CONNECTED: { label: 'Đã kết nối', className: 'text-emerald-700', icon: Radio }, RECONNECTING: { label: 'Đang kết nối lại', className: 'text-amber-700', icon: LoaderCircle }, DISCONNECTED: { label: 'Mất kết nối', className: 'text-red-700', icon: CloudOff }, REVOKED: { label: 'Phiên đăng nhập đã bị chuyển', className: 'text-red-700', icon: ShieldAlert } };
export function ConnectionStatus({ phase, compact = false, inverse = false }: { phase: ConnectionPhase; compact?: boolean; inverse?: boolean }) { const item = details[phase]; const Icon = item.icon; const spinning = phase === 'CONNECTING' || phase === 'SYNCING' || phase === 'RECONNECTING'; return <span className={cn('inline-flex items-center gap-1.5 text-xs font-bold', inverse ? 'text-white/80' : item.className)} role="status"><Icon className={cn('h-3.5 w-3.5', spinning && 'animate-spin')} />{compact && phase === 'CONNECTED' ? 'Trực tuyến' : item.label}</span>; }
