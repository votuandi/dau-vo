# Browser-visible overrides. Defaults use the Vite development proxy.
VITE_API_URL=/api
VITE_SOCKET_PATH=/socket.io

# Optional direct endpoints for deployments that do not use a reverse proxy.
# VITE_SOCKET_URL=http://localhost:3000
# VITE_DEV_SERVER_TARGET=http://localhost:3000
